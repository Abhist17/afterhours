# Demo video 2 — PreStocks bounty (target 2:15)

Covers "Best Use of PreStocks" only. Separate recording from video 1 —
judges scoring this bounty shouldn't have to sit through the whole desk
tour to find the PreStocks minute.

**Pre-load:**
- `https://afterhours-v0dr.onrender.com/dashboard/?address=6GJbPKBtovsrMEEMcic5KMi5tswh9qSyT5ZYLMqEwNgt`
  ("A real PreStocks wallet")

| Time | On screen | Say |
|---|---|---|
| 0:00–0:15 | Landing page or a blank tab with the PreStocks logo/site if you want a hook shot; otherwise skip straight to the wallet. | "PreStocks tokenizes pre-IPO equity — OpenAI, SpaceX, Anthropic — on Solana, before any of it is public. Afterhours reads it." |
| 0:15–0:35 | Load the linked wallet. Point at the five PreStocks tokens sitting next to SOL and USDC. | "This is a real mainnet wallet: five PreStocks tokens, found through the largest holders of the SpaceX token, read live — same as any other wallet, no key, no custody." |
| 0:35–1:05 | Open "What you hold." Hover a PreStocks token to show its price and the "snapshot from prestocks.com" label. | "Priced from a snapshot of PreStocks' own API — their endpoint has no browser CORS, so it's refreshed at build time, not fetched live — sitting right beside the risk-scored xStocks book, not folded into it." |
| 1:05–1:35 | Point at the label/tooltip explaining why they're excluded from VaR/score. | "A private company has no public listing, so there's no thirty-day series to score it against. The honest move is to say that, not to dress up a number that doesn't know it — so PreStocks tokens are priced and shown, never scored into the book's VaR." |
| 1:35–1:55 | Quick cut back to a plain xStocks wallet, then back to the PreStocks one, to show the same read path. | "Same pipeline as every xStock: token accounts, both token programs, verified mints — PreStocks just adds eight more rows to a list the desk already knew how to read." |
| 1:55–2:15 | End card / final frame: the wallet link on screen. | "Only PreStocks tokens are used here — no other pre-IPO token is integrated. This is Afterhours' best use of PreStocks." |

**Total: 2:15.** If you need runtime for a required minimum, extend
0:35–1:05 with a second PreStocks token (e.g. Anthropic or Anduril) rather
than adding new claims.
